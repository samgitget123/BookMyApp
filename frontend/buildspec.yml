version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing frontend dependencies...
      - npm install
  build:
    commands:
      - echo Building the frontend...
      - npm run build
artifacts:
  files:
    - '**/*'
  base-directory: build
